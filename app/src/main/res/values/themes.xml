<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Gradka" parent="android:Theme.Material.Light.NoActionBar" />

    <!--
        Native splash screen shown while the activity is starting (cold start).
        Uses the brand background + leaf logo so the launch sequence flows
        seamlessly into the in-app SplashContent before Welcome.
    -->
    <style name="Theme.Gradka.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Gradka</item>
    </style>
</resources>